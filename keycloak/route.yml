apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: keycloak
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`keycloak.deploywizard.in`)
      services:
        - name: keycloak-keycloakx-headless
          port: 80