apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: homelab-dashboard-route
spec:
  entryPoints:
    - websecure
  routes:
  - kind: Rule
    # Rule on the Host
    match: Host(`homelab.deploywizard.in`)
    services:
      - kind: Service
        name: homelab-dashboard
        port: 80